I just built my first AI Chatbot from scratch — and I did it in ONE day.
24 hours ago I had zero Python knowledge.
Today I built DecoBot — a fully functional rule-based AI chatbot that:
✅ Understands and responds to user inputs
✅ Sanitizes input using .lower() and .strip()
✅ Uses a dictionary knowledge base for instant O(1) lookups
✅ Handles unknown inputs with a smart fallback response, which is by the way, one of the best things I love about my rule based DecoBot because it makes the user understand it's usefulness before more commands are added to it's dictionary or before it is connected to an AI 
✅ Runs in a continuous loop and exits cleanly
This is Project 1 of my AI Engineering internship at Decodelabs — and it's just the beginning.
The biggest lesson I learned today:
You don't need to know everything before you start. You just need to know enough to build — then build.

